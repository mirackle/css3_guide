1 今天我才知道，闭合标签用来显示文本，自闭合标签用来引用资源内容
2 闭合标签里面的引用内容，浏览器会额外发起请求来
3 标签之间的空白会被浏览器忽略，包括制表符、回车、空格，而根据CSS标准，默认情况下，多个空白会被叠加成一个
4 块级元素会扩展到与父元素同宽，会有少量边距。这解释了为什么所有的块级元素始终占一行
5 块级元素会尽量扩展，而行内元素会尽量收缩
6 HTML实体，第一次接触这个概念：& + string + ;。left-double-qoute=ldquo,left-single-qoute=lsquo
